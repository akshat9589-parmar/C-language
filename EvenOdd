// Check Wheather the number is Even or Odd

#include <stdio.h>

int main() {

    int n;

    scanf("%d",&n);
    printf("n: %d\n",n);

    if( n % 2 ==0) {
        printf("The number is Even");
    }
    else {
        printf("The number is Odd");  
      }

    return 0;
}