#include <stdio.h>

int main() {

     int n;           // We have to print the factorial of n
     int factorial =1;

  for(int i =1; i <=a; i++) {
           factorial *= i;
  }   
  printf("Factorial: %d",factorial);
  
    return 0;
}